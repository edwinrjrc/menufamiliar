import { FiltroplatosPipe } from './filtroplatos.pipe';

describe('FiltroplatosPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroplatosPipe();
    expect(pipe).toBeTruthy();
  });
});
